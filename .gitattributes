# Scripts run through Git Bash: CRLF makes them die with "$'\r': command not found".
# This repo is cloned on Windows where core.autocrlf=true, so pin them explicitly.
*.sh   text eol=lf
*.ps1  text eol=crlf
*.md   text
